import { CopyPastNumFloatValueDirective } from './copy-past-num-float-value.directive';

describe('CopyPastNumFloatValueDirective', () => {
  it('should create an instance', () => {
    const directive = new CopyPastNumFloatValueDirective();
    expect(directive).toBeTruthy();
  });
});
