import { NumFloatDirective } from './num-float.directive';

describe('NumFloatDirective', () => {
  it('should create an instance', () => {
    const directive = new NumFloatDirective();
    expect(directive).toBeTruthy();
  });
});
